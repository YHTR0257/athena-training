[project]
name = "template"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.3",
    "pre-commit>=4.5.1",
    "pytest>=9.0.2",
    "pyyaml>=6.0.2",
    "ruff>=0.14.10",
    "ty>=0.0.9",
]
